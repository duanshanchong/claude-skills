Write tests FIRST for the following requirement, before any implementation code.

Guidelines:
- Use the project's existing test framework (check CLAUDE.md or package.json/pyproject.toml/Cargo.toml etc.)
- Cover main use cases, boundary conditions, and error cases
- Tests should be clear, descriptive, and independently runnable
- Run the tests to confirm they FAIL (red phase of TDD)
- Do NOT write implementation code yet

After writing tests, present them and wait for user confirmation before implementing.

Requirement: $ARGUMENTS
